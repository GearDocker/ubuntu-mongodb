replica_config = {
  _id: "<%= @replica_name %>",
  members: [
    <%= @mongos_replica.map {|m| "{_id: #{m[:id]}, priority: #{m[:priority]},host: \"#{m[:ip]}:#{m[:port]}\"}" }.join(',') %>
  ]
}

sleep(300);

rs.initiate(replica_config);

sleep(600);



